<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.MGSRTeam" parent="android:Theme.Material.Light.NoActionBar" />

    <!-- Splash theme: black background so there's no white flash before the video splash loads -->
    <style name="Theme.MGSRTeam.Splash" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
    </style>
</resources>