<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.MGSRTeam" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowBackground">#0F1923</item>
    </style>

    <!-- Splash theme: uses the AndroidX SplashScreen API (Option B - logo + teal accent) -->
    <style name="Theme.MGSRTeam.Splash" parent="Theme.MGSRTeam">
        <item name="android:windowSplashScreenBackground">#0F1923</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/splash_screen_icon</item>
    </style>
</resources>