<?xml version="1.0" encoding="utf-8"?>
<!-- Collapsed (single-line) custom notification content.
     Used with DecoratedCustomViewStyle — system adds the header row. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:layoutDirection="locale">

    <!-- Accent colour bar (blue/orange/green by type) -->
    <View
        android:id="@+id/notification_accent"
        android:layout_width="6dp"
        android:layout_height="44dp"
        android:layout_marginEnd="12dp"
        android:background="@drawable/notification_accent_green" />

    <!-- MGSR logo -->
    <ImageView
        android:id="@+id/notification_logo"
        android:layout_width="36dp"
        android:layout_height="36dp"
        android:src="@drawable/for_app_logo"
        android:scaleType="centerCrop"
        android:layout_marginEnd="12dp"
        android:importantForAccessibility="no" />

    <!-- Text block -->
    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical">

        <TextView
            android:id="@+id/notification_title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textAppearance="@style/TextAppearance.Compat.Notification.Title"
            android:singleLine="true"
            android:ellipsize="end" />

        <TextView
            android:id="@+id/notification_body"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textAppearance="@style/TextAppearance.Compat.Notification"
            android:singleLine="true"
            android:ellipsize="end"
            android:layout_marginTop="2dp" />

    </LinearLayout>

</LinearLayout>
